# Comparing `tmp/opentf_tools_nightly-0.43.0.dev474-py3-none-any.whl.zip` & `tmp/opentf_tools_nightly-0.43.0.dev478-py3-none-any.whl.zip`

## zipinfo {}

```diff
@@ -1,16 +1,16 @@
-Zip file size: 40815 bytes, number of entries: 14
--rw-r--r--  2.0 unx    23417 b- defN 23-Aug-01 14:55 opentf/tools/ctl.py
--rw-r--r--  2.0 unx    13215 b- defN 23-Aug-01 14:55 opentf/tools/ctlcommons.py
--rw-r--r--  2.0 unx    31854 b- defN 23-Aug-01 14:55 opentf/tools/ctlconfig.py
--rw-r--r--  2.0 unx     8878 b- defN 23-Aug-01 14:55 opentf/tools/ctlnetworking.py
--rw-r--r--  2.0 unx    11850 b- defN 23-Aug-01 14:55 opentf/tools/ctlqualitygate.py
--rw-r--r--  2.0 unx    28250 b- defN 23-Aug-01 14:55 opentf/tools/ctlworkflows.py
--rw-r--r--  2.0 unx     3188 b- defN 23-Aug-01 14:55 opentf/tools/done.py
--rw-r--r--  2.0 unx     9391 b- defN 23-Aug-01 14:55 opentf/tools/ready.py
--rw-rw-rw-  2.0 unx    11357 b- defN 23-Aug-01 14:56 opentf_tools_nightly-0.43.0.dev474.dist-info/LICENSE
--rw-r--r--  2.0 unx     1806 b- defN 23-Aug-01 14:56 opentf_tools_nightly-0.43.0.dev474.dist-info/METADATA
--rw-r--r--  2.0 unx       92 b- defN 23-Aug-01 14:56 opentf_tools_nightly-0.43.0.dev474.dist-info/WHEEL
--rw-r--r--  2.0 unx      139 b- defN 23-Aug-01 14:56 opentf_tools_nightly-0.43.0.dev474.dist-info/entry_points.txt
--rw-r--r--  2.0 unx        7 b- defN 23-Aug-01 14:56 opentf_tools_nightly-0.43.0.dev474.dist-info/top_level.txt
--rw-rw-r--  2.0 unx     1267 b- defN 23-Aug-01 14:56 opentf_tools_nightly-0.43.0.dev474.dist-info/RECORD
-14 files, 144711 bytes uncompressed, 38683 bytes compressed:  73.3%
+Zip file size: 41048 bytes, number of entries: 14
+-rw-r--r--  2.0 unx    23784 b- defN 23-Aug-01 15:37 opentf/tools/ctl.py
+-rw-r--r--  2.0 unx    13215 b- defN 23-Aug-01 15:37 opentf/tools/ctlcommons.py
+-rw-r--r--  2.0 unx    32425 b- defN 23-Aug-01 15:37 opentf/tools/ctlconfig.py
+-rw-r--r--  2.0 unx     8878 b- defN 23-Aug-01 15:37 opentf/tools/ctlnetworking.py
+-rw-r--r--  2.0 unx    11850 b- defN 23-Aug-01 15:37 opentf/tools/ctlqualitygate.py
+-rw-r--r--  2.0 unx    28442 b- defN 23-Aug-01 15:37 opentf/tools/ctlworkflows.py
+-rw-r--r--  2.0 unx     3188 b- defN 23-Aug-01 15:37 opentf/tools/done.py
+-rw-r--r--  2.0 unx     9391 b- defN 23-Aug-01 15:37 opentf/tools/ready.py
+-rw-rw-rw-  2.0 unx    11357 b- defN 23-Aug-01 15:38 opentf_tools_nightly-0.43.0.dev478.dist-info/LICENSE
+-rw-r--r--  2.0 unx     1806 b- defN 23-Aug-01 15:38 opentf_tools_nightly-0.43.0.dev478.dist-info/METADATA
+-rw-r--r--  2.0 unx       92 b- defN 23-Aug-01 15:38 opentf_tools_nightly-0.43.0.dev478.dist-info/WHEEL
+-rw-r--r--  2.0 unx      139 b- defN 23-Aug-01 15:38 opentf_tools_nightly-0.43.0.dev478.dist-info/entry_points.txt
+-rw-r--r--  2.0 unx        7 b- defN 23-Aug-01 15:38 opentf_tools_nightly-0.43.0.dev478.dist-info/top_level.txt
+-rw-rw-r--  2.0 unx     1267 b- defN 23-Aug-01 15:38 opentf_tools_nightly-0.43.0.dev478.dist-info/RECORD
+14 files, 145841 bytes uncompressed, 38916 bytes compressed:  73.3%
```

## zipnote {}

```diff
@@ -18,26 +18,26 @@
 
 Filename: opentf/tools/done.py
 Comment: 
 
 Filename: opentf/tools/ready.py
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev474.dist-info/LICENSE
+Filename: opentf_tools_nightly-0.43.0.dev478.dist-info/LICENSE
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev474.dist-info/METADATA
+Filename: opentf_tools_nightly-0.43.0.dev478.dist-info/METADATA
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev474.dist-info/WHEEL
+Filename: opentf_tools_nightly-0.43.0.dev478.dist-info/WHEEL
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev474.dist-info/entry_points.txt
+Filename: opentf_tools_nightly-0.43.0.dev478.dist-info/entry_points.txt
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev474.dist-info/top_level.txt
+Filename: opentf_tools_nightly-0.43.0.dev478.dist-info/top_level.txt
 Comment: 
 
-Filename: opentf_tools_nightly-0.43.0.dev474.dist-info/RECORD
+Filename: opentf_tools_nightly-0.43.0.dev478.dist-info/RECORD
 Comment: 
 
 Zip file comment:
```

## opentf/tools/ctl.py

```diff
@@ -52,14 +52,21 @@
 
 DEFAULT_NAMESPACE = 'default'
 
 
 ########################################################################
 # Help messages
 
+GETNAMESPACES_COMMAND = 'get namespaces'
+GETSUBSCRIPTIONS_COMMAND = 'get subscriptions'
+DELETESUBSCRIPTION_COMMAND = 'delete subscription _'
+GETAGENTS_COMMAND = 'get agents'
+DELETEAGENT_COMMAND = 'delete agent _'
+GETCHANNELS_COMMAND = 'get channels'
+
 GENERAL_HELP = '''opentf-ctl controls the OpenTestFactory orchestrators.
 
  Find more information at: https://opentestfactory.org/tools/running-commands
 
 Basic Commands:
   get workflows                    List active and recent workflows
   run workflow {filename}          Start a workflow
@@ -640,39 +647,39 @@
 
 def print_help(args: List[str]) -> None:
     """Display help."""
     if _is_command('options', args):
         print(OPTIONS_HELP)
     if _is_command('version', args):
         print(VERSION_HELP)
-    elif _is_command('get subscriptions', args):
+    elif _is_command(GETSUBSCRIPTIONS_COMMAND, args):
         print(GET_SUBSCRIPTIONS_HELP)
     elif _is_command('delete subscription', args):
         print(DELETE_SUBSCRIPTION_HELP)
     elif _is_command('generate token', args):
         print(GENERATE_TOKEN_HELP)
     elif _is_command('view token', args):
         print(VIEW_TOKEN_HELP)
     elif _is_command('check token', args):
         print(VALIDATE_TOKEN_HELP)
-    elif _is_command('get agents', args):
+    elif _is_command(GETAGENTS_COMMAND, args):
         print(GET_AGENTS_HELP)
-    elif _is_command('get channels', args):
+    elif _is_command(GETCHANNELS_COMMAND, args):
         print(GET_CHANNELS_HELP)
     elif _is_command('delete agent', args):
         print(DELETE_AGENT_HELP)
     elif _is_command('config', args):
         print_config_help(args)
     elif _is_command('_ workflow', args) or _is_command('_ workflows', args):
         print_workflow_help(args)
     elif _is_command('get qualitygate', args) or _is_command(
         'describe qualitygate', args
     ):
         print_qualitygate_help(args)
-    elif _is_command('get namespaces', args):
+    elif _is_command(GETNAMESPACES_COMMAND, args):
         print(GET_NAMESPACES_HELP)
     elif len(args) == 2:
         print(GENERAL_HELP)
     else:
         _error('Unknown command.  Use --help to list known commands.')
         sys.exit(1)
 
@@ -723,40 +730,44 @@
             )
             sys.exit(2)
         check_token(sys.argv[3], sys.argv[5])
         sys.exit(0)
     if _is_command('check token _', sys.argv):
         _error('Missing required parameter.  Use "check token --help" for details.')
         sys.exit(2)
-    if _is_command('get namespaces', sys.argv):
-        _ensure_options('get namespaces', sys.argv[1:], extra=[('--selector', '-l')])
+    if _is_command(GETNAMESPACES_COMMAND, sys.argv):
+        _ensure_options(
+            GETNAMESPACES_COMMAND, sys.argv[1:], extra=[('--selector', '-l')]
+        )
         read_configuration()
         list_namespaces()
-    elif _is_command('get subscriptions', sys.argv):
-        _ensure_options('get subscriptions', sys.argv[1:], extra=[('--output', '-o')])
+    elif _is_command(GETSUBSCRIPTIONS_COMMAND, sys.argv):
+        _ensure_options(
+            GETSUBSCRIPTIONS_COMMAND, sys.argv[1:], extra=[('--output', '-o')]
+        )
         read_configuration()
         list_subscriptions()
-    elif _is_command('delete subscription _', sys.argv):
-        subscription_id = _ensure_options('delete subscription _', sys.argv[1:])
+    elif _is_command(DELETESUBSCRIPTION_COMMAND, sys.argv):
+        subscription_id = _ensure_options(DELETESUBSCRIPTION_COMMAND, sys.argv[1:])
         read_configuration()
         delete_subscription(subscription_id)
-    elif _is_command('get agents', sys.argv):
-        _ensure_options('get agents', sys.argv[1:], extra=[('--output', '-o')])
+    elif _is_command(GETAGENTS_COMMAND, sys.argv):
+        _ensure_options(GETAGENTS_COMMAND, sys.argv[1:], extra=[('--output', '-o')])
         read_configuration()
         list_agents()
-    elif _is_command('get channels', sys.argv):
+    elif _is_command(GETCHANNELS_COMMAND, sys.argv):
         _ensure_options(
-            'get channels',
+            GETCHANNELS_COMMAND,
             sys.argv[1:],
             extra=[('--output', '-o'), ('--field-selector', '--selector', '-l')],
         )
         read_configuration()
         list_channels()
-    elif _is_command('delete agent _', sys.argv):
-        agent_id = _ensure_options('delete agent _', sys.argv[1:])
+    elif _is_command(DELETEAGENT_COMMAND, sys.argv):
+        agent_id = _ensure_options(DELETEAGENT_COMMAND, sys.argv[1:])
         read_configuration()
         delete_agent(agent_id)
     elif _is_command('_ workflow', sys.argv) or _is_command('_ workflows', sys.argv):
         workflow_cmd()
     elif _is_command('get qualitygate', sys.argv) or _is_command(
         'describe qualitygate', sys.argv
     ):
```

## opentf/tools/ctlconfig.py

```diff
@@ -33,14 +33,25 @@
 CONFIG = {}
 HEADERS = {}
 
 
 ########################################################################
 # Help messages
 
+CONFIG_GENERATE_COMMAND = 'config generate'
+CONFIG_VIEW_COMMAND = 'config view'
+CONFIG_SETCREDENTIALS_COMMAND = 'config set-credentials _'
+CONFIG_DELETECREDENTIALS_COMMAND = 'config delete-credentials _'
+CONFIG_USECONTEXT_COMMAND = 'config use-context _'
+CONFIG_SETCONTEXT_COMMAND = 'config set-context _'
+CONFIG_DELETECONTEXT_COMMAND = 'config delete-context _'
+CONFIG_SETORCHESTRATOR_COMMAND = 'config set-orchestrator _'
+CONFIG_DELETEORCHESTRATOR_COMMAND = 'config delete-orchestrator _'
+
+
 CONFIG_HELP = '''Modify opentfconfig files using subcommands like "opentf-ctl config use-context my-context"
 
  The following rules are used to find the configuration file to use:
 
  1.  If the --opentfconfig flag is set, then that file is loaded.
  2.  If $OPENTF_CONFIG environment variable is set, then it is used as a file path and that file is loaded.
  3.  Otherwise, ${HOME}/.opentf/config is used.
@@ -356,117 +367,14 @@
 ########################################################################
 # Commands
 
 
 ## config commands
 
 
-def config_cmd() -> None:
-    """Interact with opentf-config.
-
-    Possible sub commands are
-        generate             Generate configuration file from user inputs
-        set-context          Set a context entry in the opentf-config
-        set-orchestrator     Set an orchestrator entry in the opentf-config
-        set-credentials      Set a user entry in the opentf-config
-        delete-context       Unset a context entry in the opentf-config
-        delete-orchestrator  Unset an orchestrator entry in the opentf-config
-        delete-credentials   Unset a user entry in the opentf-config
-        view                 Display current opentf-config
-    """
-    if _is_command('config generate', sys.argv):
-        _ensure_options(
-            'config generate',
-            sys.argv[1:],
-            extra=[
-                ('--name',),
-                ('--orchestrator-server',),
-                ('--orchestrator-receptionist-port',),
-                ('--orchestrator-observer-port',),
-                ('--orchestrator-eventbus-port',),
-                ('--orchestrator-agentchannel-port',),
-                ('--orchestrator-killswitch-port',),
-                ('--orchestrator-qualitygate-port',),
-                ('--insecure-skip-tls-verify',),
-            ],
-        )
-        generate_config()
-    elif _is_command('config use-context _', sys.argv):
-        context = _ensure_options('config use-context _', sys.argv[1:])
-        use_context(context)
-    elif _is_command('config set-context _', sys.argv):
-        context = _ensure_options(
-            'config set-context _',
-            sys.argv[1:],
-            extra=[
-                ('--namespace',),
-            ],
-        )
-        set_context(context)
-    elif _is_command('config delete-context _', sys.argv):
-        context = _ensure_options('config delete-context _', sys.argv[1:])
-        delete_context(context)
-    elif _is_command('config set-orchestrator _', sys.argv):
-        orchestrator = _ensure_options(
-            'config set-orchestrator _',
-            sys.argv[1:],
-            extra=[
-                ('--server',),
-                ('--receptionist-port',),
-                ('--observer-port',),
-                ('--eventbus-port',),
-                ('--agentchannel-port',),
-                ('--killswitch-port',),
-                ('--qualitygate-port',),
-                ('--insecure-skip-tls-verify',),
-                ('--receptionist-prefix',),
-                ('--observer-prefix',),
-                ('--eventbus-prefix',),
-                ('--agentchannel-prefix',),
-                ('--killswitch-prefix',),
-                ('--qualitygate-prefix',),
-                ('--receptionist-force-base-url',),
-                ('--observer-force-base-url',),
-                ('--eventbus-force-base-url',),
-                ('--agentchannel-force-base-url',),
-                ('--killswitch-force-base-url',),
-                ('--qualitygate-force-base-url',),
-                ('--insecure-skip-tls-verify',),
-            ],
-        )
-        set_orchestrator(orchestrator)
-    elif _is_command('config delete-orchestrator _', sys.argv):
-        orchestrator = _ensure_options('config delete-orchestrator _', sys.argv[1:])
-        delete_orchestrator(orchestrator)
-    elif _is_command('config set-credentials _', sys.argv):
-        credentials = _ensure_options(
-            'config set-credentials _',
-            sys.argv[1:],
-            extra=[
-                ('--step-depth',),
-                ('--job-depth',),
-                ('--max-command-length',),
-            ],
-        )
-        set_credentials(credentials)
-    elif _is_command('config delete-credentials _', sys.argv):
-        credentials = _ensure_options('config delete-credentials _', sys.argv[1:])
-        delete_credentials(credentials)
-    elif _is_command('config view', sys.argv):
-        _ensure_options('config view', sys.argv[1:])
-        view_config()
-    elif len(sys.argv) == 2:
-        print_config_help(sys.argv)
-    else:
-        _error(
-            'Unknown subcommand.  Use "opentf-ctl config --help" to list known subcommands.'
-        )
-        sys.exit(1)
-
-
 def _write_opentfconfig(conf_filename: str, config: Dict[str, Any]) -> None:
     with open(conf_filename, 'w', encoding='utf-8') as conffile:
         yaml.safe_dump(config, conffile)
 
 
 def _read_opentfconfig() -> Tuple[str, Dict[str, Any]]:
     conf_filename = (
@@ -873,15 +781,15 @@
     print('#')
 
     print(yaml.dump(generated_conf))
 
 
 def print_config_help(args: List[str]) -> None:
     """Display config help."""
-    if _is_command('config generate', args):
+    if _is_command(CONFIG_GENERATE_COMMAND, args):
         print(CONFIG_GENERATE_HELP)
     elif _is_command('config use-context', args):
         print(CONFIG_USE_CONTEXT_HELP)
     elif _is_command('config set-context', args):
         print(CONFIG_SET_CONTEXT_HELP)
     elif _is_command('config set-orchestrator', args):
         print(CONFIG_SET_ORCHESTRATOR_HELP)
@@ -889,14 +797,117 @@
         print(CONFIG_SET_CREDENTIALS_HELP)
     elif _is_command('config delete-context', args):
         print(CONFIG_DELETE_CONTEXT_HELP)
     elif _is_command('config delete-orchestrator', args):
         print(CONFIG_DELETE_ORCHESTRATOR_HELP)
     elif _is_command('config delete-credentials', args):
         print(CONFIG_DELETE_CREDENTIALS_HELP)
-    elif _is_command('config view', args):
+    elif _is_command(CONFIG_VIEW_COMMAND, args):
         print(CONFIG_VIEW_HELP)
     elif _is_command('config', args):
         print(CONFIG_HELP)
     else:
         _error('Unknown config command.  Use --help to list known commands.')
         sys.exit(1)
+
+
+def config_cmd() -> None:
+    """Interact with opentf-config.
+
+    Possible sub commands are
+        generate             Generate configuration file from user inputs
+        set-context          Set a context entry in the opentf-config
+        set-orchestrator     Set an orchestrator entry in the opentf-config
+        set-credentials      Set a user entry in the opentf-config
+        delete-context       Unset a context entry in the opentf-config
+        delete-orchestrator  Unset an orchestrator entry in the opentf-config
+        delete-credentials   Unset a user entry in the opentf-config
+        view                 Display current opentf-config
+    """
+    if _is_command(CONFIG_GENERATE_COMMAND, sys.argv):
+        _ensure_options(
+            CONFIG_GENERATE_COMMAND,
+            sys.argv[1:],
+            extra=[
+                ('--name',),
+                ('--orchestrator-server',),
+                ('--orchestrator-receptionist-port',),
+                ('--orchestrator-observer-port',),
+                ('--orchestrator-eventbus-port',),
+                ('--orchestrator-agentchannel-port',),
+                ('--orchestrator-killswitch-port',),
+                ('--orchestrator-qualitygate-port',),
+                ('--insecure-skip-tls-verify',),
+            ],
+        )
+        generate_config()
+    elif _is_command(CONFIG_USECONTEXT_COMMAND, sys.argv):
+        context = _ensure_options(CONFIG_USECONTEXT_COMMAND, sys.argv[1:])
+        use_context(context)
+    elif _is_command(CONFIG_SETCONTEXT_COMMAND, sys.argv):
+        context = _ensure_options(
+            CONFIG_SETCONTEXT_COMMAND,
+            sys.argv[1:],
+            extra=[
+                ('--namespace',),
+            ],
+        )
+        set_context(context)
+    elif _is_command(CONFIG_DELETECONTEXT_COMMAND, sys.argv):
+        context = _ensure_options(CONFIG_DELETECONTEXT_COMMAND, sys.argv[1:])
+        delete_context(context)
+    elif _is_command(CONFIG_SETORCHESTRATOR_COMMAND, sys.argv):
+        orchestrator = _ensure_options(
+            CONFIG_SETORCHESTRATOR_COMMAND,
+            sys.argv[1:],
+            extra=[
+                ('--server',),
+                ('--receptionist-port',),
+                ('--observer-port',),
+                ('--eventbus-port',),
+                ('--agentchannel-port',),
+                ('--killswitch-port',),
+                ('--qualitygate-port',),
+                ('--insecure-skip-tls-verify',),
+                ('--receptionist-prefix',),
+                ('--observer-prefix',),
+                ('--eventbus-prefix',),
+                ('--agentchannel-prefix',),
+                ('--killswitch-prefix',),
+                ('--qualitygate-prefix',),
+                ('--receptionist-force-base-url',),
+                ('--observer-force-base-url',),
+                ('--eventbus-force-base-url',),
+                ('--agentchannel-force-base-url',),
+                ('--killswitch-force-base-url',),
+                ('--qualitygate-force-base-url',),
+                ('--insecure-skip-tls-verify',),
+            ],
+        )
+        set_orchestrator(orchestrator)
+    elif _is_command(CONFIG_DELETEORCHESTRATOR_COMMAND, sys.argv):
+        orchestrator = _ensure_options(CONFIG_DELETEORCHESTRATOR_COMMAND, sys.argv[1:])
+        delete_orchestrator(orchestrator)
+    elif _is_command(CONFIG_SETCREDENTIALS_COMMAND, sys.argv):
+        credentials = _ensure_options(
+            CONFIG_SETCREDENTIALS_COMMAND,
+            sys.argv[1:],
+            extra=[
+                ('--step-depth',),
+                ('--job-depth',),
+                ('--max-command-length',),
+            ],
+        )
+        set_credentials(credentials)
+    elif _is_command(CONFIG_DELETECREDENTIALS_COMMAND, sys.argv):
+        credentials = _ensure_options(CONFIG_DELETECREDENTIALS_COMMAND, sys.argv[1:])
+        delete_credentials(credentials)
+    elif _is_command(CONFIG_VIEW_COMMAND, sys.argv):
+        _ensure_options(CONFIG_VIEW_COMMAND, sys.argv[1:])
+        view_config()
+    elif len(sys.argv) == 2:
+        print_config_help(sys.argv)
+    else:
+        _error(
+            'Unknown subcommand.  Use "opentf-ctl config --help" to list known subcommands.'
+        )
+        sys.exit(1)
```

## opentf/tools/ctlworkflows.py

```diff
@@ -81,14 +81,19 @@
 REFRESH_DELAY = 10
 
 MAX_COMMAND_LENGTH = 15
 
 ########################################################################
 # Help messages
 
+GETWORKFLOWS_COMMAND = 'get workflows'
+GETWORKFLOW_COMMAND = 'get workflow _'
+RUNWORKFLOW_COMMAND = 'run workflow _'
+KILLWORKFLOW_COMMAND = 'kill workflow _'
+
 RUN_WORKFLOW_HELP = '''Start a workflow
 
 Examples:
   # Start the workflow defined in my_workflow.yaml
   opentf-ctl run workflow my_workflow.yaml
 
   # Start the workflow and wait until it completes
@@ -799,34 +804,34 @@
 # Helpers
 
 
 def print_workflow_help(args: List[str]):
     """Display help."""
     if _is_command('run workflow', args):
         print(RUN_WORKFLOW_HELP)
-    elif _is_command('get workflows', args):
+    elif _is_command(GETWORKFLOWS_COMMAND, args):
         print(GET_WORKFLOWS_HELP)
     elif _is_command('get workflow', args):
         print(GET_WORKFLOW_HELP)
     elif _is_command('kill workflow', args):
         print(KILL_WORKFLOW_HELP)
     else:
         _error('Unknown command.  Use --help to list known commands.')
         sys.exit(1)
 
 
 def workflow_cmd():
     """Interact with workflows."""
-    if _is_command('get workflows', sys.argv):
-        _ensure_options('get workflows', sys.argv[1:], extra=[('--output', '-o')])
+    if _is_command(GETWORKFLOWS_COMMAND, sys.argv):
+        _ensure_options(GETWORKFLOWS_COMMAND, sys.argv[1:], extra=[('--output', '-o')])
         read_configuration()
         list_workflows()
-    elif _is_command('run workflow _', sys.argv):
+    elif _is_command(RUNWORKFLOW_COMMAND, sys.argv):
         workflow = _ensure_options(
-            'run workflow _',
+            RUNWORKFLOW_COMMAND,
             sys.argv[1:],
             extra=[
                 ('--step-depth', '-s'),
                 ('--job-depth', '-j'),
                 ('--max-command-length', '-c'),
                 ('--namespace', '-n'),
                 ('--selector', '-l', '--field-selector'),
@@ -839,17 +844,17 @@
                 ('--wait', '--watch', '-w'),
                 ('--show-notifications', '-a'),
                 ('--verbose', '-v'),
             ],
         )
         read_configuration()
         run_workflow(workflow)
-    elif _is_command('get workflow _', sys.argv):
+    elif _is_command(GETWORKFLOW_COMMAND, sys.argv):
         workflow_id = _ensure_options(
-            'get workflow _',
+            GETWORKFLOW_COMMAND,
             sys.argv[1:],
             extra=[
                 ('--step-depth', '-s'),
                 ('--job-depth', '-j'),
                 ('--max-command-length', '-c'),
                 ('--output', '-o'),
                 ('--selector', '-l', '--field-selector'),
@@ -858,14 +863,14 @@
                 ('--watch', '-w'),
                 ('--show-notifications', '-a'),
                 ('--verbose', '-v'),
             ],
         )
         read_configuration()
         get_workflow(workflow_id, '--watch' in sys.argv or '-w' in sys.argv)
-    elif _is_command('kill workflow _', sys.argv):
-        workflow_id = _ensure_options('kill workflow _', sys.argv[1:])
+    elif _is_command(KILLWORKFLOW_COMMAND, sys.argv):
+        workflow_id = _ensure_options(KILLWORKFLOW_COMMAND, sys.argv[1:])
         read_configuration()
         kill_workflow(workflow_id)
     else:
         _error('Unknown command.  Use --help to list known commands.')
         sys.exit(1)
```

## Comparing `opentf_tools_nightly-0.43.0.dev474.dist-info/LICENSE` & `opentf_tools_nightly-0.43.0.dev478.dist-info/LICENSE`

 * *Files identical despite different names*

## Comparing `opentf_tools_nightly-0.43.0.dev474.dist-info/METADATA` & `opentf_tools_nightly-0.43.0.dev478.dist-info/METADATA`

 * *Files 0% similar despite different names*

```diff
@@ -1,10 +1,10 @@
 Metadata-Version: 2.1
 Name: opentf-tools-nightly
-Version: 0.43.0.dev474
+Version: 0.43.0.dev478
 Summary: OpenTestFactory Orchestrator Tools
 Home-page: https://opentestfactory.org/tools/
 Author: Martin Lafaix
 Author-email: mlafaix@henix.com
 Maintainer: Henix
 Maintainer-email: opentestfactory@henix.com
 License: Apache Software License (https://www.apache.org/licenses/LICENSE-2.0)
```

